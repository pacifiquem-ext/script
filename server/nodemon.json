{
  "watch": ["src", "../packages/shared/src"],
  "ext": "ts",
  "exec": "pnpm --filter @script/shared build && node -r dotenv/config -r @swc-node/register src/index.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
